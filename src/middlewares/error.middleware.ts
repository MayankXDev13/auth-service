/**
 * @deprecated — thin re-export, canonical is `src/lib/http/errorMiddleware.ts`.
 * Previously 49 LoC with .js suffix inconsistency (`../logger/winston.logger.js`), now deep http module owns JSON contract.
 */
export { errorHandler } from '../lib/http';
